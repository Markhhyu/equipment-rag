历史会话：
{history_text}

当前用户问题：{query}

请根据历史会话和当前问题，提取用户正在询问的商品名称（item_names）。
1. 【最高优先级】如果当前问题明确出现了商品名称或型号，必须使用当前问题中的名称或型号；
   即使历史会话讨论过其他设备，也绝对不能用历史设备替换当前问题中的新设备。
2. 只有当前问题使用代词或省略设备名（如“这个”“它”“怎么复位”）时，才能结合历史会话做指代消解。
3. 可能有一个或多个商品名称，但不能重复；如果无法确定，item_names 返回空列表。
4. 请重新改写当前问题（rewritten_query），使其成为包含商品名称的独立完整问题。
5. item_names和rewritten_query中的设备必须完全一致，禁止item_names写LJ2268、rewritten_query却写PT770之类的冲突结果。
6. 不得凭空补充当前问题和历史会话中都没有出现过的故障、用途或操作意图。

请直接返回JSON格式结果，格式如下：
{{
    "item_names": ["商品A", "商品B"],
    "rewritten_query": "关于商品A和商品B，..."
}}
