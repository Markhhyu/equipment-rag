# Grafana日志数据源自动配置。Loki只在Docker内部暴露给Grafana，浏览器无需直连。
apiVersion: 1

datasources:
  - name: Equipment RAG Loki
    type: loki
    uid: equipment-rag-loki
    access: proxy
    url: http://loki:3100
    isDefault: false
    editable: false
    jsonData:
      maxLines: 2000
